{% include 'default/_form.html.twig' with {
    page_title: 'title_komentarz_edit',
    page_title_param: {'%id%': komentarz.id},
    attr: {
        method: 'PUT',
        action: url('komentarz_edit', {'id': komentarz.id}),
    },
    form: form,
    back_to_list_path: 'komentarz_index',
} %}