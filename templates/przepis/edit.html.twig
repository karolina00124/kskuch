{% include 'default/_form.html.twig' with {
    page_title: 'title_przepis_edit',
    page_title_param: {'%id%': przepis.id},
    attr: {
        method: 'PUT',
        action: url('przepis_edit', {'id': przepis.id}),
    },
    form: form,
    back_to_list_path: 'przepis_index',
} %}